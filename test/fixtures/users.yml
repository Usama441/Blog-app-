# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:

  # Devise requires email not be empty; the unique index on users.email
  # will raise if multiple fixtures end up with the same value ("" by
  # default). Provide distinct addresses so fixtures can load cleanly.
  email: one@example.com

  # If you need other fields (password, etc.) they can be added here.
  # Only constraints (not validations) are enforced during fixture load.

two:
  email: two@example.com
  # column: value
